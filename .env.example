# Copy to .env and fill in your values. Do not commit .env.

# Full path to this project (used when setting up crontab and Launch Agent plist)
# Example: /Users/yourname/food-automation — use the output of: pwd (when inside the project)
PROJECT_PATH=/path/to/food-automation

# URL of the Google form used for login (browser opens here for you to sign in)
LOGIN_FORM_URL=https://forms.gle/YOUR_LOGIN_FORM_ID

# URL of the food submission form
SUBMIT_FORM_URL=https://docs.google.com/forms/d/e/YOUR_FORM_ID/viewform

# Your email (used to select your name in the form checkbox, e.g. "Record you@company.com")
USER_EMAIL=your.email@example.com

# Food choice option text (exact text of the radio option, e.g. "Nonveg" or "Veg")
FOOD_CHOICE=Nonveg
